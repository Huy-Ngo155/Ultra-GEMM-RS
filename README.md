# Ultra-GEMM-RS
A high-performance CPU GEMM engine written in Rust featuring multi-level cache tiling (L1/L2/L3), Huge Page memory allocation, and architecture-specific SIMD micro-kernels (AVX-512, NEON, AMX). Optimized for zero-overhead matrix operations with NUMA-aware scheduling
